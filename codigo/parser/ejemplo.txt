VAR z : FLOAT;

FUNC INT parte1(INT repeticiones)

  VAR switch : BOOL;
  VAR i : INT;
{
  switch = true;
}

FUNC FLOAT parte2(FLOAT repeticiones)
  VAR i : INT;
  VAR switch : BOOL;
{
  switch = true;
}

CANCION (90)
  VAR x : FLOAT;
{
  x = 20;
  WHILE((x/10)>1){
    PLAY(A1,4);
  };
  FOR(x=0;x==3;x=x+3;){
    PLAY(G4,2);
  };
  x= CALL parte1(15);;
}
